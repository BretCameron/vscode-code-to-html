{
  "name": "vscode-code-to-html",
  "displayName": "VSCode Extension: Code to HTML",
  "description": "Copy syntax-highlighted code as HTML for blog posts, forum submissions and more!",
  "version": "0.0.1",
  "author": {
    "name": "Bret Cameron",
    "url": "https://bretcameron.com/"
  },
  "publisher": "bret",
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "codeToHtml.copyAsHtml",
        "title": "Copy as HTML",
        "category": "Code to HTML"
      },
      {
        "command": "codeToHtml.selectTheme",
        "title": "Select Theme",
        "category": "Code to HTML"
      },
      {
        "command": "codeToHtml.toggleLineNumbers",
        "title": "Toggle Line Numbers",
        "category": "Code to HTML"
      },
      {
        "command": "codeToHtml.selectFilePath",
        "title": "Select File Path Display",
        "category": "Code to HTML"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "codeToHtml.copyAsHtml",
          "group": "5_cutcopypaste",
          "when": "resourceScheme == file"
        }
      ],
      "editor/context": [
        {
          "command": "codeToHtml.copyAsHtml",
          "group": "9_cutcopypaste"
        }
      ]
    },
    "configuration": {
      "title": "Code to HTML",
      "properties": {
        "codeToHtml.theme": {
          "type": "string",
          "default": "github-dark",
          "enum": [
            "github-dark",
            "github-light",
            "dracula",
            "nord",
            "one-dark-pro",
            "monokai",
            "solarized-dark",
            "solarized-light",
            "night-owl",
            "catppuccin-mocha"
          ],
          "description": "Syntax highlighting theme"
        },
        "codeToHtml.lineNumbers": {
          "type": "boolean",
          "default": false,
          "description": "Include line numbers"
        },
        "codeToHtml.showFilePath": {
          "type": "string",
          "default": "filename",
          "enum": [
            "filename",
            "relative",
            "absolute",
            "none"
          ],
          "description": "File path display in headers"
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "package": "npx @vscode/vsce package"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.2.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "shiki": "^1.24.0"
  }
}